import streamlit as st

# إعدادات الصفحة الأساسية
st.set_page_config(page_title="اختبار العلوم - الأستاذ أحمد حاتم فودة", layout="centered")

# تصميم الهيدر (العنوان)
st.markdown("""
    <div style="background-color: #6c63ff; padding: 20px; border-radius: 10px; text-align: center; margin-bottom: 25px;">
        <h1 style="color: white; margin: 0;">اختبار العلوم التفاعلي 🎓</h1>
        <p style="color: white; font-size: 1.2rem; opacity: 0.9;">إعداد الأستاذ: أحمد حاتم فودة</p>
    </div>
""", unsafe_allow_html=True)

# السؤال (بناءً على صورتك السابقة)
st.subheader("3- أي من المواد التالية يُعد مصدراً للطاقة المتجددة؟")

# عرض الخيارات كأزرار راديو
options = ["أ) الفحم", "ب) النفط", "ج) الطاقة الشمسية", "د) الغاز الطبيعي"]
choice = st.radio("اختر الإجابة الصحيحة من القائمة أدناه:", options)

# زر التأكيد
if st.button("تأكيد الإجابة"):
    if "ج)" in choice:
        st.success("إجابة صحيحة! أحسنت يا بطل ✅")
        st.balloons() # حركة احتفالية بسيطة
    else:
        st.error("إجابة خاطئة! حاول مرة أخرى ❌")
        st.info("الإجابة الصحيحة هي: ج) الطاقة الشمسية")

# زر للانتقال (شكلي حالياً)
st.button("السؤال التالي ←")
